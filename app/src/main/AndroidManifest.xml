<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.naat.yaapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.YaApp">
        <activity
            android:name=".presentation.activities.BuyRechargeActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.activities.DashboardActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.activities.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>